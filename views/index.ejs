<!DOCTYPE html>
<html lang="en">
  <head>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <title>URL Shortener</title>
  </head>
  <body class="bg-dark">
    <div class="container py-5">
      <h1 class="text-center text-white mb-4">URL Shortener</h1>
      <div class="row justify-content-center">
        <div class="col-lg-6">
          <form action="/url" method="post" class="mb-4">
            <div class="input-group">
              <input
                type="url"
                name="originalURL"
                class="form-control"
                placeholder="Enter URL"
                required
              />
              <button type="submit" class="btn btn-primary">Shorten</button>
            </div>
          </form>
          <% if (shortID){ %>
          <div class="alert alert-success text-center">
            <h2 class="h4">Shortened URL</h2>
            <p class="mb-0">Here's your shortened URL, click to visit:</p>
              <a class="mb-0 text-decoration-none text-danger" href="<%= process.env.BASE_URL + '/' + shortID %>" target="_blank"
                ><%= process.env.BASE_URL + '/' + shortID %></a
              >
            </p>
            <p class="mb-0">To analyze your URL, please visit:</p>
              <a class="mb-0 text-decoration-none text-danger" href="<%= process.env.BASE_URL + '/url/analytics/' + shortID %>" target="_blank"
                ><%= process.env.BASE_URL + '/url/analytics/' + shortID %></a
              >
            </p>
          </div>
          <% } %>
        </div>
      </div>
    </div>
    <a href="https://github.com/yourusername/yourproject" target="_blank" style="position: fixed; right: 50px; bottom: 20px; color: white;">
      <i class="fab fa-github fa-3x"></i>
    </a>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
